@Library('commons')
import net.courtanet.jenkins.Utils

String project = "cloud-platforms"
String script = "stop-vms.yml"
String task = "gcp"
String workspace = pwd()
String repositoryUrl = "https://github.com/joehmchiu/${project}.git"
String branch = "master"
String msg = "VMs powered off"

node() {
    stage 'Confirm'
    timeout(time: 30, unit: 'SECONDS') {
        input msg + "?"
    }
}

