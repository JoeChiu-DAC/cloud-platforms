node {

    project = "cloud-platforms"
    script = "stop-vms.yml"
    def task = "gcp"
    def workspace = pwd()
    def repositoryUrl = "https://github.com/joehmchiu/${project}.git"
    def branch = "master"
    def msg = "VMs powered off"

    load "/opt/bin/jenkins-jobs.groovy"
}
